# [已弃用] 如何在 aaPanel (宝塔) 部署 (PHP版)

**注意**: 本项目目前推荐使用 Node.js 部署 (见 `DEPLOY_AAPANEL_NODE.md`)。

如果您必须使用 PHP 部署（仅静态文件 + PHP 存储），请使用旧版代码。当前代码库已针对 Node.js 优化。

如果您坚持使用 PHP，请确保将 `server.js` 及其相关逻辑替换回 PHP 逻辑。